class Example {

    class NestedExample {

        int a, b = 0;
        boolean e = true, f = false;
        int g = a + b;

        int doSomething(int c, boolean d) {
            a = a + b;
            if (a > b) {
                a = a - b;
            } else {
                a = a + b;
            }
        }
    }

    int a, b = 0;
    boolean e = true, f = false;
    int g = a + b;

    int doSomething(int c, boolean d) {
        a = a + b;
        if (a > b) {
            a = a - b;
        } else {
            a = a + b;
        }
    }
}